# 产品需求文档（PRD）

> **项目名称**：个人工作台 —— 影视内容辅助创作工具
>
> **版本**：V1.0（含 V1.1 / V1.2 演进规划）
>
> **目标用户**：自由职业者（编剧 / 自媒体创作者 / 短视频创作者）
>
> **产品定位**：一款桌面端工具，将剧情大纲与人物小传自动转化为剧本，串联生图、视频生成、语音合成等外部 AI 模型，最终输出带有画面和配音的完整视频片段。

---

## 目录

- [1. 产品概述](#1-产品概述)
  - [1.1 解决的问题](#11-解决的问题)
  - [1.2 目标用户画像](#12-目标用户画像)
  - [1.3 产品形态](#13-产品形态)
  - [1.4 核心工作流](#14-核心工作流)
- [2. 使用指南](#2-使用指南)
  - [2.1 首次使用配置](#21-首次使用配置)
  - [2.2 标准创作流程](#22-标准创作流程)
  - [2.3 高级功能（V1.2+）](#23-高级功能v12)
- [3. 功能需求详述](#3-功能需求详述)
  - [3.1 项目管理](#31-项目管理)
  - [3.2 剧本生成](#32-剧本生成)
  - [3.3 资产库](#33-资产库)
  - [3.4 角色书（V1.2+）](#34-角色书v12)
  - [3.5 场景标注（V1.2+）](#35-场景标注v12)
  - [3.6 分镜管理](#36-分镜管理)
  - [3.7 生图模型接入](#37-生图模型接入)
  - [3.8 视频生成模型接入](#38-视频生成模型接入)
  - [3.9 语音模型接入](#39-语音模型接入)
  - [3.10 视频合成与导出](#310-视频合成与导出)
  - [3.11 全局设置](#311-全局设置)
  - [3.12 数据备份与恢复](#312-数据备份与恢复)
- [4. 页面与交互设计](#4-页面与交互设计)
- [5. 数据模型](#5-数据模型)
- [6. 技术方案](#6-技术方案)
- [7. 开发计划](#7-开发计划)
- [8. 附录](#8-附录)

---

## 1. 产品概述

### 1.1 解决的问题

当前自由创作者要制作一段带剧情的短视频，需要在多个 AI 工具之间反复横跳：用一个工具写剧本、用另一个工具生图、再用另一个工具生成视频片段、最后还得单独配音和合成。整个过程割裂，文件散落各处，创作上下文不连续。

**个人工作台**将这套流程整合为一个桌面应用，让创作者在一个界面内完成从剧本到成片的完整闭环。

### 1.2 目标用户画像

| 用户类型 | 典型场景 | 核心诉求 |
|---------|---------|---------|
| 自媒体短视频创作者 | 日更剧情短片，需要快速产出 | 效率优先，批量生成，风格统一 |
| 独立编剧 / 分镜师 | 将剧本可视化，做 previz 预演 | 角色场景一致性，分镜精度 |
| 自由职业者（综合） | 接单制作宣传片、解说视频 | 低成本，一个工具覆盖全流程 |

### 1.3 产品形态

Electron 桌面应用，支持 Windows / macOS。无需联网即可使用本地功能（剧本编辑、数据管理、视频合成等），仅调用外部 AI 模型时需联网。

**核心设计原则**：
- **本地优先**：项目数据、媒体文件全部本地存储，不上传任何数据到第三方服务器（用户主动配置的 AI 模型 API 除外）
- **模型无关**：所有 AI 能力通过通用 HTTP API 接入，用户自主选择模型供应商，应用不做绑定
- **渐进复杂度**：V1.0 提供简单可用的完整闭环；V1.2 引入角色书、场景标注等高级锚定机制，可选升级

### 1.4 核心工作流

```
┌─────────────────────────────────────────────────────────────┐
│                      标准创作流程（V1.0）                     │
│                                                             │
│  剧情大纲                                                    │
│  + 人物小传                                                  │
│     │                                                       │
│     ▼                                                       │
│  大纲审查 → 生成剧本 → 拆分分镜                              │
│     │                                                       │
│     ▼                                                       │
│  资产库生图 ────────────────────┐                            │
│  ├─ 人物设定图（锚定角色形象）  │                            │
│  ├─ 场景概念图（锚定环境氛围）  │                            │
│  └─ 分镜画面图 ←───────────────┘                            │
│     │                                                       │
│     ▼                                                       │
│  视频生成（图 + 运动提示词 → 视频片段）                       │
│     │                                                       │
│     ▼                                                       │
│  配音生成（TTS 对白 → 角色配音）                             │
│     │                                                       │
│     ▼                                                       │
│  合成导出（视频拼接 + 配音叠加 + 字幕 → MP4）                 │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   高级锚定流程（V1.2+）                       │
│                                                             │
│  在 V1.0 基础上，引入双锚定机制：                             │
│                                                             │
│  人物锚定（角色书）          场景锚定（JSON 标注）            │
│  ├─ 多角度参考图             ├─ 硬约束：坐标 + 深度 + 层级   │
│  ├─ 结构化外观字段           ├─ 软约束：风格/材质标签         │
│  └─ 一次定义，全局引用       └─ 一次标注，所有分镜复用        │
│                                                             │
│  分镜引擎升级为「三类提示词」输出：                           │
│  ├─ 环境提示词（场景风格 + 镜头参数）                        │
│  ├─ 角色提示词（ref_id 指向角色书，不重写）                   │
│  └─ 动作/特效提示词（动态要素 + 区域隔离）                    │
│                                                             │
│  新增时间轴锚点（timeline），为音效/口型同步预留骨架          │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 使用指南

> 本章从用户视角描述如何操作软件完成创作。对应的技术实现细节见 §3。

### 2.1 首次使用配置

**第一步：安装与启动**

下载安装包（Windows .exe / macOS .dmg），安装后启动应用，进入空白主页。

**第二步：配置 AI 模型**

点击顶部工具栏齿轮图标 ⚙，打开「全局设置」弹窗。至少需要配置以下模型之一才能开始创作：

| 模型类别 | 用途 | 推荐配置项 |
|---------|------|-----------|
| LLM（大语言模型）| 生成剧本、审查大纲 | API Base URL + Key + 模型名称 |
| 生图模型 | 人物设定图、场景图、分镜图 | API Base URL + Key + 默认尺寸 |
| 视频模型 | 图转视频 | API Base URL + Key + 默认时长 |
| 语音模型（可选）| TTS 配音 | API Base URL + Key + 默认音色 |

应用内置几套主流模型的参数模板（OpenAI 兼容接口、Stable Diffusion WebUI、Runway、Pika 等），选择模板后自动适配请求格式。也支持「自定义」模板。

**第三步：指定 FFmpeg（可选）**

如需导出 MP4，需要 FFmpeg。应用内置 FFmpeg 二进制，也可在设置中手动指定本地 FFmpeg 路径。

---

### 2.2 标准创作流程

#### 2.2.1 创建项目

1. 点击左侧面板底部「+ 新建项目」按钮。
2. 弹窗中输入项目名称（如"古宅探秘第1集"），确认后项目出现在左侧列表。
3. 项目自动打开，主编辑区进入「大纲」Tab。

#### 2.2.2 编写大纲与人物小传

**编写大纲**：

在「大纲」Tab 的文本框内编写剧情大纲。大纲是自由文本，无字数限制。例如：

> 深夜，独行探险家林墨潜入一座荒废百年的欧式古宅。大厅中，他发现地面上刻满了发光的符文，符文的蓝光逐渐变得剧烈。突然，门自动关闭，一个巨大的阴影从楼梯上方蔓延下来……

**添加人物小传**：

1. 点击大纲编辑区下方的「添加人物」按钮。
2. 弹窗中填写人物信息：

| 字段 | 说明 | 必填 |
|------|------|------|
| 姓名 | 角色名称 | ✓ |
| 性别 | 男 / 女 / 其他 | |
| 年龄 | 数字或年龄段 | |
| 性格描述 | 如"沉着冷静，话少但句句关键" | |
| 背景故事 | 角色过往经历 | |
| 外观特征 | 如"黑色短发，深蓝风衣，身高180cm，面容清瘦" | 推荐填写 |

3. 确认后人物卡片出现在人物列表中。可添加多个人物。
4. 已添加的人物卡片支持编辑和删除。

#### 2.2.3 大纲智能审查

在生成剧本之前，系统可以帮助检查大纲质量：

1. 点击「审查大纲」按钮，系统将大纲和人物列表发送给 LLM 分析。
2. 审查结果以信息面板展示在大纲编辑区上方，分为三级：

- 🔴 **必须处理**：如"大纲提到'林墨'但未添加该人物的小传"
- 🟡 **建议完善**：如"'林墨'的外观特征为空，建议补充以确保生图一致性"
- 🔵 **优化提示**：如"古宅场景可增加外部环境描写，丰富分镜画面感"

3. 每条提示可点击定位。可逐条处理或点击「一键添加缺失人物」批量创建。
4. 审查不是阻塞操作——即使有未处理警告，仍可直接生成剧本。

#### 2.2.4 生成剧本

1. 确认大纲和人物小传已就绪，点击「生成剧本」按钮。
2. 系统显示 loading 状态，大约 30-60 秒后返回结果。
3. 剧本以结构化形式展示：

```
剧本标题
├── 第 1 场
│   ├── 地点：古宅大门外
│   ├── 时间：深夜
│   ├── 场景描述：月光下的哥特式古宅，铁门半掩……
│   ├── 出场人物：林墨
│   ├── 动作描述：林墨推开铁门，手电筒光束扫过前厅
│   └── 对白
│       └── 林墨："就是这里了。"
├── 第 2 场
│   └── ...
```

4. 如果 LLM 返回格式异常，系统提示「返回格式异常，请重试」，并展示原始返回文本供手动复制。

**使用建议**：大纲写得越具体（场景细节、人物动作、关键台词方向），生成的剧本越符合预期。如果结果不满意，修改大纲后重新生成，或在剧本编辑器中手动修改。

#### 2.2.5 编辑剧本

生成后的剧本可以手动修改：

- **修改场次信息**：点击地点、时间、场景描述直接编辑
- **修改/删除对白**：点击对白文本编辑，或点击删除按钮移除整句
- **删除整场戏**：在场次标题行点击删除
- **添加新场次**：点击「添加场次」，手动填写场次信息

所有编辑自动保存。

#### 2.2.6 拆分与管理分镜

1. 剧本生成后，切换到「分镜」Tab。
2. 系统自动将每场戏拆分为一个或多个分镜。拆分逻辑为：每场戏至少一个分镜；包含明显动作转换或场景切换的场次拆为多个分镜。
3. 分镜列表展示，每个分镜显示：

| 信息 | 说明 |
|------|------|
| 序号 | 分镜编号（S01, S02...） |
| 缩略图 | 生成后显示，待生成状态显示占位图标 |
| 描述 | 基于剧本自动生成的分镜描述 |
| 状态标签 | 🟡待生成 / ⏳生成中 / 🟢已完成 / 🔴失败 |

4. **手动调整分镜**：
   - 点击分镜进入编辑弹窗，可修改描述、添加运动提示词
   - 拖拽调整分镜顺序
   - 增删分镜

**[V1.0] 分镜提示词**：每个分镜携带单一的 `imagePrompt`（生图提示词）和 `videoPrompt`（运动提示词），用户在分镜编辑弹窗中手动填写或修改。

**[V1.2+] 三类提示词**：升级为 §2.3.3 中描述的锚定式提示词引擎，详见对应章节。

#### 2.2.7 资产库：创建人物设定图

切换到「资产库」Tab →「人物设定」子 Tab。系统已根据人物小传自动创建了条目。

1. 在左侧列表点击目标角色（如"林墨"）。
2. 右侧显示角色信息卡片和提示词输入框。系统自动填充推荐提示词：
   > 角色「林墨」全身站立设定图，黑色短发，深蓝风衣，身高180cm，面容清瘦，正面视角，概念艺术风格，精细服饰细节，纯色背景，高分辨率
3. 用户可修改提示词，点击「生成」调用生图模型；**或**点击「导入图片」从本地选择已有图片直接作为设定图。
4. 生成/导入结果在右侧预览。满意点击「设为设定图」保存，不满意重新生成或重新导入。
5. 已保存的设定图作为该角色的**主设定图**，后续分镜生图时自动引用。

**为什么先做设定图？** 确保同一角色在不同分镜中的长相、服装、发型保持一致。如果你已有现成的角色设计图（如手绘稿、3D 渲染图），直接导入比 AI 生成更精准。

**使用建议**：为每个主要角色准备设定图（生成或导入均可）。配角可用纯文本描述代替，但画面一致性会下降。

#### 2.2.8 资产库：创建场景概念图

切换到「场景设定」子 Tab。

1. 点击「添加场景」，填写场景名称（如"古宅大厅"）和环境描述。
2. 系统自动组装提示词，调用生图模型生成概念图；或点击「导入图片」使用本地已有图片。
3. 保存后的场景概念图在分镜生图时自动匹配引用。

**使用建议**：至少为每个主要场景准备一张概念图（生成或导入）。概念图定义场景的整体氛围、光照风格和建筑特征，帮助后续分镜保持环境一致。如果你有实景照片或参考图，直接导入通常比 AI 生成更可靠。

#### 2.2.9 资产库：分镜生图

切换到「分镜生图」子 Tab。这是从分镜到画面的关键步骤。

1. 顶部下拉选择目标项目，下方列表展示该项目所有分镜及其状态。
2. 点击某个分镜，右侧展示：
   - **分镜信息**：场景描述、出场人物
   - **自动匹配的参考资产**：系统自动从资产库匹配对应的人物设定图和场景概念图，如「📎 林墨设定图 · 古宅大厅概念图」
   - **提示词输入框**：系统根据分镜描述 + 匹配资产自动填充推荐提示词，用户可手动增删参考资产和修改提示词
   - **生图参数**：尺寸、风格等
3. 点击「生成」调用生图模型。
4. 支持「单张生成」和「批量生成整个项目」。
5. 生成结果存入对应分镜，分镜 Tab 中同步刷新预览。

**自动匹配规则**：
- 分析分镜的「出场人物」列表，在资产库中按名称匹配人物设定图
- 分析分镜的「场景描述」文本，与场景概念图的名称和描述做关键词匹配
- 匹配成功则自动勾选为参考图，未匹配则仅使用文本提示词

#### 2.2.9b 资产库：导入风格参考图

风格参考图用于统一所有分镜的视觉画风。例如导入一张赛博朋克风格参考图，所有生成的画面都会偏向霓虹灯、冷色调、强对比的质感。

1. 切换到资产库 →「风格参考」子 Tab。
2. 点击「导入风格图」，从本地选择一张风格图片（PNG / JPG / WebP）。
3. 填写风格名称和描述；可选调整**风格强度**滑块（0–1），强度越高画面越偏离提示词原意而靠近风格图。
4. 导入后，在分镜生图时可选择该风格参考作为全局或单分镜的风格锚定条件。
5. 在分镜生图界面中：
   - 点击「风格参考」下拉框，选择已导入的风格图
   - 或在项目的分镜生图设置中指定「默认风格」，所有分镜自动继承

**使用建议**：风格参考图应选择能代表目标画风的典型画面（色彩方案 + 光影风格 + 笔触质感），而非具体的角色或场景。过于复杂的风格图可能导致模型风格迁移不精准。

#### 2.2.10 生成视频

在分镜 Tab 中，对每个已有图片的分镜生成视频。

1. 点击分镜旁的「生成视频」按钮。
2. 弹出视频生成设置弹窗：
   - **运动提示词**：描述画面如何运动，如"镜头缓慢推进，符文的蓝光脉动，阴影从楼梯向下蔓延"
   - **时长**：默认 5 秒
3. 点击「生成」，系统调用视频模型。异步任务显示「⏳ 生成中...」。
4. 完成后分镜列表中显示「视频已生成」，可在右侧预览区播放。
5. 不满意可点击「重新生成」。

**使用建议**：运动提示词应聚焦于画面内的动态元素（人物动作、光影变化、物体运动），而非镜头语言（镜头运动由分镜描述覆盖）。如果是静态画面（如定场镜头），运动提示词可以简单写"微妙光影变化，氛围感"。

#### 2.2.11 生成配音

在分镜 Tab 或剧本视图中，为角色对白生成配音。

1. 在全局设置中为每个角色预设音色（或使用默认音色）。
2. 生成方式：
   - **单条生成**：点击对白旁的「生成配音」按钮
   - **按场次批量生成**：点击场次标题旁的「批量配音」
   - **按角色批量生成**：为该角色所有对白一次性生成
3. 生成后对白旁出现播放按钮 ▶，点击可试听。
4. 不满意可单条重新生成。

**长文本处理**：部分 TTS 接口有文本长度限制，超限时系统自动分段生成再拼接。

#### 2.2.12 合成导出

所有分镜视频和配音就绪后，最后一步。

1. 点击工具栏「合成导出」按钮。
2. 弹出导出设置弹窗：

| 设置项 | 默认值 | 说明 |
|--------|--------|------|
| 输出分辨率 | 1080p | 可选 720p / 1080p / 4K |
| 帧率 | 24fps | |
| 添加字幕 | 开启 | 根据对白文本自动生成 ASS 字幕 |
| 字幕样式 | 默认 | 字体大小、颜色、位置 |
| 输出路径 | 用户选择 | |

3. 点击「开始合成」，显示进度条 + 预估剩余时间。
4. 合成完成，弹出提示「导出完成」。可选择打开文件所在文件夹。

**合成步骤**（系统自动完成）：
1. 将所有分镜视频按顺序拼接
2. 将各段配音音频按时间轴对齐叠加
3. 根据对白文本生成 ASS 字幕文件，编码嵌入视频流
4. 输出 H.264 + AAC 的 MP4 文件

---

### 2.3 高级功能（V1.2+）

> 以下功能在 V1.0 中不可用，标记为 V1.2 升级目标。在 V1.0 中，用户通过资产库的「人物设定图」和「场景概念图」以简化方式实现类似效果。

#### 2.3.1 角色书管理

角色书是人物设定图的升级版。它不仅包含参考图，还包含结构化的外观字段和多角度图集。

**创建角色书**：

1. 在资产库 Tab 中，右键点击已有的人物设定图条目，选择「升级为角色书」；或在「角色书」子 Tab 中手动创建。
2. 角色书编辑界面包含：

| 区域 | 内容 | 说明 |
|------|------|------|
| 基础信息 | 姓名、性别、年龄 | 从人物小传同步 |
| 外观描述 | 结构化字段：发型、脸型、肤色、体型、标志性特征 | 比自由文本更精确，用于组装提示词 |
| 服装约束 | 常驻服装描述、颜色、款式 | 确保跨镜头服装不变 |
| 参考图集 | 多角度图上传 | 见下方说明 |
| 姿态参考 | 站姿、行走姿态等描述或参考图 | 可选 |

**多角度图管理**：

| 图位 | 用途 | 重要性 |
|------|------|--------|
| 正面全身照 | 主设定图，分镜引用首选 | 必须 |
| 侧面照 | 侧身镜头参考 | 推荐 |
| 半身近景 | 中景/特写镜头参考 | 推荐 |
| 关键表情特写 | 情感表达统一 | 可选 |
| 背面照 | 背面镜头参考 | 按需 |

**角色书的使用**：分镜生图时，系统通过 `ref_id` 引用角色书。根据当前分镜的镜头角度（如侧面特写），系统自动选择最匹配的参考角度图 + 组装外观描述文本作为角色提示词。用户无需每次重写角色提示词。

**V1.0 升级路径**：V1.0 的 `CharacterAsset`（单图 + 文字描述）可一键迁移为角色书。原有设定图自动成为正面全身照，外观描述从自由文本字段提取，其他角度留空待用户补全。

#### 2.3.2 场景结构化标注

场景标注将场景从"一张概念图"升级为"一套结构化空间数据"。

**标注界面**：

1. 在资产库 Tab 中，右键点击已有场景概念图，选择「场景标注」进入标注模式。
2. 标注界面分为三栏：
   - **左侧工具栏**：标注工具选择（BBox 绘制、深度赋值、光源标记、标签添加）
   - **中央画布**：场景概念图叠加标注层，支持缩放和拖拽
   - **右侧属性面板**：选中标注对象的属性编辑

**硬约束标记**（不可变的精确约束）：

| 操作 | 说明 |
|------|------|
| 绘制 BBox | 在画布上拖拽绘制矩形，标记门、窗、大型家具等关键结构物体的位置和尺寸 |
| 设置 depth | 在属性面板调整深度值（0=最远，1=最近），或使用滚轮直接调整 |
| 设置 layer | 设定 Z 排序层级，数字越大越靠前（如桌子 layer=2，人物 layer=3，人物在桌子前面） |
| 标记遮挡 | 为物体标注 `occlusion` 类型：none / partial / full |
| 标记交互 | 选择两个不同 layer 的物体，定义接触关系（如手放在桌面上的接触点坐标） |

**软约束标签**（允许浮动的风格描述）：

在右侧面板的标签区，为场景添加关键词标签：

| 标签类别 | 示例 |
|---------|------|
| 风格 | medieval, gothic, abandoned |
| 材质 | stone_wall, wooden_floor, iron_gate |
| 氛围 | dusty, dim, solemn, mysterious |
| 细节 | cobweb, cracked_tile, faded_mural |

**标注保存与复用**：标注数据存储为独立 JSON 文件（见 §5.7），与场景概念图关联。分镜引用场景时，系统读取 JSON 标注自动组装环境提示词，保证同一场景不同镜头角度下空间结构不变。

**按需补标**：
- 分镜设计时，系统检测镜头角度相对基准视角的偏移量
- 偏移超过 90° 时，标记该分镜为"标注未覆盖"
- 用户可在该分镜上点击「补充标注」，为盲区追加标注数据
- 补充标注块与原始标注合并存储，后续分镜自动继承

#### 2.3.3 锚定式分镜提示词引擎

V1.2 的分镜不再使用单一提示词，而是自动产出**三类提示词**，并携带镜头参数和时间轴。

**工作方式**：

1. 用户在分镜编辑界面填写分镜描述（如"中景，林墨侧身站在大厅中央，低头凝视地面符文"）。
2. 系统自动解析描述中的要素并分发到三类提示词：
   - **环境提示词**：从场景标注 JSON 读取硬约束 + 软约束，结合描述中的镜头参数，组装为完整环境提示词
   - **角色提示词**：通过 `ref_id` 从角色书提取对应人物的外观描述 + 最匹配角度的参考图，无需重写
   - **动作/特效提示词**：从描述中提取动态要素（如"符文渐亮""阴影蔓延"），显式生成特效提示词
3. 三类提示词各自携带 `region` 作用域标记：
   - 环境 → `full`（全域生效）
   - 角色 → `person_bbox`（仅人物区域）
   - 特效 → `fx_mask`（仅特效区域）
4. 用户可逐类调整提示词文本，或切换回「简化模式」使用单一提示词。

**区域隔离（防污染）**：提示词输入模型时，`region` 字段告知调度层哪些词作用于哪些区域。当前模型不支持 Regional Prompting 时，`region` 和 `attention_isolation` 字段闲置；一旦模型升级支持，直接启用无需改结构。

#### 2.3.4 时间轴编辑

每个分镜包含一条 `timeline`，用于标注画面内各事件的发生时刻，为未来音效和口型同步预留。

**编辑方式**：

1. 在分镜编辑弹窗中，下方展示时间轴轨道。
2. 轨道上可添加事件节点：

| 事件类型 | 示例 | 必填参数 | 可选参数 |
|---------|------|---------|---------|
| 动作（action）| "主角推开门" | t_ms, duration_ms | sync_hint（匹配音效标签） |
| 特效（fx）| "符文渐亮" | t_ms, duration_ms | intensity_curve, sync_hint |
| 对白（dialogue）| "林墨：这是什么..." | t_ms, duration_ms | tts_anchor=true（触发口型同步） |

3. 拖拽调整事件的时间位置和持续时长。
4. `sync_hint` 是语义标签（如 `door_creak`），不绑定具体音频文件——未来无论接入哪种音效引擎，靠标签匹配即可对齐。

---

## 3. 功能需求详述

> 本章面向研发，每个模块按「用户操作步骤 → 实现逻辑 → 边界情况」结构描述。

### 3.1 项目管理

#### 3.1.1 功能说明

项目是创作流程的最小管理单元。一个项目包含从大纲到最终成片的所有数据。

#### 3.1.2 操作

| 操作 | 描述 | 实现方式 |
|------|------|---------|
| 新建项目 | 输入项目名称，创建空白项目 | 弹窗输入名称，生成 UUID，写入 projects.json |
| 打开项目 | 从左侧列表点击，主编辑区加载全部数据 | 切换当前活跃 projectId，Zustand store 更新 |
| 删除项目 | 删除项目及其关联媒体文件 | 弹窗二次确认，主进程删除媒体目录 + 从 projects.json 移除 |
| 重命名项目 | 修改项目名称 | 右键菜单或双击触发内联编辑 |

#### 3.1.3 边界情况

- 项目名称为空：提示「请输入项目名称」
- 删除当前打开的项目：自动切换到下一个项目或空白页
- 同名项目：不做限制（以 UUID 区分），但建议前端提示

---

### 3.2 剧本生成

#### 3.2.1 大纲编辑

**用户操作**：在「大纲」Tab 的 textarea 中自由输入文本。

**实现逻辑**：
- 前端使用受控组件，每次变更自动通过 IPC 保存到本地 JSON
- 自动保存防抖 500ms，避免频繁写入

#### 3.2.2 人物小传管理

**用户操作**：点击「添加人物」→ 弹窗填写 → 确认后人物卡片出现在列表中。卡片可编辑和删除。

**实现逻辑**：
- 人物数据作为 `Character[]` 存储在项目 JSON 中
- 添加人物时，系统自动在资产库的「人物设定」分类下创建同名资产条目（`CharacterAsset`），`characterId` 关联到人物 UUID
- 删除人物时，不自动删除关联的资产条目（资产库独立于项目），由用户手动决定

#### 3.2.3 大纲智能审查

**用户操作**（见 §2.2.3）：点击「审查大纲」，查看三级提示列表，逐条或批量处理。

**实现逻辑**：

1. 前端拼接审查 Prompt，调用 LLM：

```
System: 你是一位资深剧本审稿人。请严格按以下 JSON 格式输出：
{ "issues": [{ "severity": "critical|warning|suggestion", "category": "missing_character|incomplete_profile|plot_gap|scene_suggestion", "title": "...", "description": "...", "relatedCharacter": "...", "suggestion": "..." }], "summary": "整体评价" }

User: 
## 剧情大纲
{outline}

## 已有人物小传
{逐一列出：姓名、性别、性格、背景、外观}

## 审查要求
1. 从大纲中提取所有出现的人物名称，逐一与已有人物小传比对
2. 检查大纲的故事结构是否完整
3. 检查已有人物的小传信息是否充分
4. 对关键人物缺少小传的情况标记为 critical
```

2. 解析 LLM 返回 JSON，按 severity 分三级渲染：
   - `critical` → 🔴 必须处理
   - `warning` → 🟡 建议完善
   - `suggestion` → 🔵 优化提示

3. 「一键添加缺失人物」：遍历所有 category 为 `missing_character` 的 issue，批量创建 Character 条目，姓名预填 `relatedCharacter`。

4. 实时联动：用户添加/编辑人物后，前端重新比对审查结果列表，已处理的条目自动消除或变灰。

**边界情况**：
- 大纲为空：弹出「请先填写剧情大纲」
- 无问题：展示绿色面板「✅ 大纲审查通过」
- API 失败：错误提示，不影响已有编辑内容
- 大纲超长：自动截断并提示「大纲超出审查限制，已截取前 N 字」
- 大数量人物（20+）：审查聚焦高频出现的关键人物，其余标记为「次要人物，可选添加」

#### 3.2.4 剧本生成 LLM 调用

**用户操作**（见 §2.2.4）：点击「生成剧本」，等待返回，在编辑器中查看和修改。

**实现逻辑**：

1. **组装 System Prompt**（内置，不可自定义）：

> 你是一位专业影视编剧。根据用户提供的剧情大纲和人物小传，生成一场或多场完整的剧本。请严格按照 JSON 格式输出：
> ```json
> { "title": "剧本标题", "scenes": [{ "sceneNumber": 1, "location": "...", "timeOfDay": "...", "description": "...", "characters": ["角色名"], "actionDescription": "...", "dialogues": [{ "characterName": "...", "text": "...", "order": 1 }] }] }
> ```
> 要求：对白自然生动，符合人物性格；场景描述简洁但画面感强；每场戏有明确的戏剧冲突或推进。

2. **组装 User Prompt**：

```
## 剧情大纲
{outline}

## 人物小传
{逐一列出：姓名、性别、年龄、性格、背景、外观}
```

3. **调用 LLM**：
   - 请求方式：`POST {baseUrl}/chat/completions`（兼容 OpenAI 格式）
   - 参数：`max_tokens: 4096`, `temperature: 0.8`, `response_format: { type: "json_object" }`
   - 超时：120s

4. **解析返回**：
   - 尝试 `JSON.parse(response)` 
   - 解析成功 → 写入项目 `script` 字段，前端渲染为可编辑剧本视图
   - 解析失败 → 展示错误提示 + 原始返回文本，用户可手动复制

5. **剧本编辑**：前端将 Script JSON 渲染为可编辑 UI，修改任意字段后自动通过 IPC 保存。

**边界情况**：
- 大纲为空：校验拦截，提示「请先填写剧情大纲」
- 人物列表为空：允许生成（纯剧情驱动），User Prompt 中注明「无指定人物」
- LLM 返回格式异常：JSON parse 容错，失败时展示原始文本
- 超时：120s 后提示用户检查 API 配置或网络

---

### 3.3 资产库

#### 3.3.1 功能定位

资产库是与「大纲」「剧本」「分镜」同级别的独立 Tab，是**所有视觉资产的管理入口**。设定图有两个来源：**AI 生成**（调用生图模型）或**本地导入**（用户已有图片）。资产库中的设定图**全局共享**，一次创建即可被多个项目复用。

#### 3.3.2 资产分类

| 分类 | 说明 | 图片来源 | 存储路径 |
|------|------|---------|---------|
| 👤 人物设定 | 角色全身站立设定图 | AI 生成 或 本地导入 | `assets/人物设定/{名称}/` |
| 🏙️ 场景设定 | 关键场景环境概念图 | AI 生成 或 本地导入 | `assets/场景设定/{名称}/` |
| 📦 物品设计 | 关键道具/物品设计图 | AI 生成 或 本地导入 | `assets/物品设计/{名称}/` |
| 🔊 音色 | 角色配音音色，含试听片段和 TTS 参数 | 手动创建（填写音色 ID + 参数）或从 TTS 引擎预设导入 | `assets/音色/{名称}/` |
| 🎨 风格参考 | 外部风格图，用于全局或分镜风格锚定 | 仅本地导入（不经 AI 生成） | `assets/风格参考/{名称}/` |
| 🎞️ 分镜生图 | 各项目分镜对应画面 | 仅 AI 生成 | 存入对应项目的分镜媒体目录 |

#### 3.3.3 人物设定图

**用户操作**（见 §2.2.7）：

- **AI 生成方式**：选择角色条目 → 编辑提示词 → 生成 → 保存为设定图。
- **本地导入方式**：选择角色条目 → 点击「导入图片」→ 从本地选择 PNG/JPG/WebP → 直接保存为设定图，跳过 AI 生成。

**实现逻辑**：

1. **条目自动创建**：`Character` 创建时，触发 hook 在 AssetStore 中创建对应 `CharacterAsset`，`characterId` 关联。
2. **AI 生成路径**：系统自动组装提示词 `角色「{name}」全身站立设定图，{appearance}，正面视角，概念艺术风格，精细服饰细节，纯色背景，高分辨率` → 调用 §3.7 生图接口。
3. **本地导入路径**：用户选择本地文件 → 前端校验格式和尺寸 → 主进程复制文件到 `assets/人物设定/{name}/design.png` → AssetStore JSON 更新 `designImagePath` + `source: 'imported'`。
4. 两种路径互不冲突：如果已有导入图，再点击 AI 生成会覆盖；反之亦然。覆盖前弹窗确认。

**边界情况**：
- 同一角色多张设定图：默认最新一张为主图，用户可右键「设为主设定图」
- 导入图片格式不支持：提示「请导入 PNG / JPG / WebP 格式的图片」
- 导入图片过大：自动压缩至 5MB 以内
- 参考图不存在：降级为纯文本提示词生图，toast 提示
- 资产文件体积：自动压缩（JPEG/WebP，质量 85%），单张 ≤ 5MB；总计超 500MB 提示清理

#### 3.3.4 场景概念图

**用户操作**（见 §2.2.8）：

- **AI 生成方式**：添加场景 → 填写名称和描述 → 生成概念图。
- **本地导入方式**：添加场景 → 填写名称和描述 → 点击「导入图片」选择本地文件 → 直接保存。

**实现逻辑**：
- AI 生成：提示词组装 `场景「{name}」，{description}，概念艺术风格，环境氛围，高分辨率`
- 本地导入：复制文件 + 标记 `source: 'imported'`
- 其余同人物设定图逻辑

#### 3.3.5 物品设计图

**用户操作**：

- **AI 生成方式**：添加物品 → 填写名称、描述、关联角色 → 生成设计图。
- **本地导入方式**：添加物品 → 填写名称、描述、关联角色 → 导入本地图片。

**实现逻辑**：
- 物品可关联到角色（`relatedCharacterIds`），在分镜生图时根据出场人物自动匹配
- 图片来源支持 AI 生成和本地导入双路径，逻辑同人物设定图

#### 3.3.6 音色资产

**功能说明**：音色资产是独立于角色的 TTS 音色配置，包含音色 ID、试听片段和语速/音调参数。角色通过 `voiceId` 引用音色资产。一处创建，跨项目复用。

**设计原则**：
- **V1.0（数据层）**：建立 `VoiceAsset` 数据表，实现基础 CRUD 和试听生成。角色编辑时从音色资产下拉选择绑定。
- **V1.2+（UI 聚合）**：在角色书界面中，系统通过 `voiceId` 自动拉取关联的音色资产，与外观、背景故事并列展示。导出角色卡时将音色配置一并打包。

**用户操作（V1.0）**：

1. 切换到资产库 →「音色」子 Tab。
2. 点击「添加音色」，填写：
   - **音色名称**：人类可读标签（如"深沉男声-林墨"）
   - **TTS 引擎**：下拉选择（OpenAI TTS / Edge TTS / 自定义）
   - **音色 ID**：TTS 引擎中的音色标识符（如 OpenAI 的 `"onyx"`）
   - **语速**：0.5–2.0，默认 1.0
   - **音调**：-20 到 +20，默认 0
   - **试听文本**：用于生成试听片段的一句话（默认"你好，我是{音色名称}"）
3. 点击「生成试听」调用 TTS 引擎生成短音频，缓存到本地。
4. 保存后，该音色在角色编辑时可选。

**角色绑定方式**：
- 编辑角色（人物小传弹窗）时，底部新增「配音设置」区域
- 下拉框列出所有已创建的 `VoiceAsset`，按名称选择
- 选中后显示该音色的试听按钮 ▶，点击可预览
- 角色 JSON 中仅存储 `voiceId: "uuid-of-voice-asset"`

**V1.2 升级路径**：
- 进入角色书界面时，系统通过 `voiceId` 查询 `VoiceAsset` 表
- 在角色书的「声音」维度展示音色名称、试听按钮、语速/音调参数
- 导出角色卡时，将 `VoiceAsset` 完整数据 + 试听音频文件一并打包，角色卡成为一个完全独立的实体，导入即可用

**边界情况**：
- 音色 ID 由用户手动填写（不同 TTS 引擎的 ID 格式不同），不自动校验
- 试听生成失败：提示「试听生成失败，请检查 TTS 配置和音色 ID」，不影响保存
- 删除音色资产时，检查是否被角色引用，若有则弹窗提示引用清单
- 同一引擎下音色名称不可重复

#### 3.3.7 分镜生图（V1.0 简化模式）

**用户操作**（见 §2.2.9）：选择项目和分镜 → 查看自动匹配的参考资产 → 编辑提示词 → 单张或批量生成。

**实现逻辑**：

1. **参考资产自动匹配**：
   - 读取分镜的 `characters` 列表，在 AssetStore 中按 `characterId` / `name` 匹配人物设定图
   - 读取分镜关联场次的 `location` 和 `description`，与场景概念图的名称/描述做 TF-IDF 关键词匹配
   - 匹配得分超过阈值的自动勾选为参考图

2. **提示词自动填充**：
   ```
   {分镜描述}
   人物：{逐一列出匹配到的角色名称和外观特征}
   场景：{匹配到的场景描述}
   画面风格：{从场景概念图标签继承}
   ```

3. **批量生成**：
   - 用户勾选多个分镜，点击「批量生成」
   - 依次调用生图接口（非并行，避免触发 API 速率限制）
   - 每完成一张通过 IPC 通知前端刷新对应分镜的预览
   - 支持跳过 `done` 状态的分镜

4. **生成结果**：图片存入 `projects/{projectId}/media/{storyboardId}.png`，Storyboard 的 `imagePath` 更新。

**边界情况**：
- 资产条目存在但未生成设定图：仅用文本提示词，toast「建议先生成设定图」
- 设定图与分镜风格差异：设定图仅作形象参考，最终风格由分镜提示词控制
- API 错误：跳过当前分镜继续下一个，错误分镜标记为 `failed`

#### 3.3.8 风格参考图

**功能说明**：用户可导入外部风格图片（如赛博朋克色调参考、水墨画风参考、吉卜力风格参考），作为全局或分镜级别的风格锚定条件，与生图提示词一同送入模型，统一画面风格。

**用户操作**：

1. 切换到资产库 →「风格参考」子 Tab。
2. 点击「导入风格图」，从本地选择图片文件（支持 PNG / JPG / WebP）。
3. 填写风格名称（如"赛博朋克霓虹风"）和风格描述（如"高饱和霓虹灯光、冷色调暗部、强对比度"）。
4. 可选设置**风格强度**（0–1，默认 0.7），控制风格迁移的力度。
5. 导入后，该风格参考在资产库中可用。

**使用方式**：

| 应用范围 | 操作 | 说明 |
|---------|------|------|
| **全局风格** | 在某个项目的分镜生图设置中，下拉选择「默认风格参考」 | 该项目所有分镜生图时自动携带此风格图 |
| **场景风格** | 在场景设定条目中，关联一个风格参考 | 该场景关联的分镜自动继承此风格，覆盖全局风格 |
| **单分镜风格** | 在分镜生图时，手动添加/更换风格参考图 | 最大灵活度，覆盖全局和场景风格 |

**优先级规则**：单分镜 > 场景 > 全局。即单分镜指定的风格图覆盖场景风格，场景风格覆盖全局风格。

**实现逻辑**：

1. **风格图传入模型**：风格参考图以 Base64 编码，根据模型支持的方式传入：
   - 支持 IP-Adapter / StyleDrop 的模型：作为风格条件图，与文本提示词并行输入
   - 支持 Reference-only ControlNet 的模型：走 ControlNet reference 通道，仅提取风格特征
   - 支持 img2img 的模型：风格图作为 init image，搭配适中 denoising strength（0.4–0.6）做风格迁移
   - 以上均不支持的模型：降级为在文本提示词中追加风格描述短语（如"in the style of cyberpunk neon"）

2. **风格强度控制**：`styleStrength` 参数映射到各模型的对应参数：
   - IP-Adapter：`scale` 参数
   - ControlNet reference：`control_weight` 参数
   - img2img：`denoising_strength` 参数（反向映射：强度越高，denoising 越低）

3. **分镜生图时的风格继承**：
   - 遍历当前分镜 → 检查是否有单分镜指定的风格图
   - 无 → 检查分镜关联场景是否有场景级风格图
   - 无 → 检查项目是否有全局默认风格图
   - 无 → 纯文本提示词生图（无风格锚定）

**边界情况**：
- 导入的图片格式不支持：提示「请导入 PNG / JPG / WebP 格式的图片」
- 风格图文件过大：自动压缩至 5MB 以内
- 模型三种风格接入方式均不支持：降级为文本追加模式，toast 提示「当前模型不支持风格图输入，已自动转为文本描述」
- 删除风格参考：检查是否被项目/场景关联引用，若有则提示并展示引用清单

---

### 3.4 角色书（V1.2+）

> **[V1.2+]** 本章描述的功能在 V1.0 中不可用。V1.0 用户通过资产库的「人物设定图」（§3.3.3）以简化方式实现角色形象管理。

#### 3.4.1 角色书 vs 人物设定图的区别

| 维度 | 人物设定图（V1.0） | 角色书（V1.2+） |
|------|-------------------|-----------------|
| 参考图数量 | 1 张主设定图 | 多角度图集（正面/侧面/半身/表情/背面） |
| 外观描述 | 自由文本 | 结构化字段（发型/脸型/肤色/体型/标志性特征） |
| 服装约束 | 无单独约束 | 常驻服装 + 颜色 + 款式独立描述 |
| 声音 | 角色表中存 `voiceId` 引用音色资产 | 通过 `voiceId` 自动拉取 VoiceAsset 完整数据，聚合展示 |
| 提示词生成 | 每次手动编写 | 自动组装，ref_id 引用 |
| 跨模型可移植性 | 只存素材+描述，编码按需提取 | 同左，声音资产同步打包 |
| 导出 | 设定图 + 文字描述 | 设定图 + 结构化 JSON + 音色配置 + 试听音频 → 完整独立实体 |

#### 3.4.2 创建角色书

**升级路径**（从 V1.0 迁移）：
1. 右键点击资产库中已有的人物设定图条目，选择「升级为角色书」
2. 系统自动迁移：原有设定图 → 正面全身照；`appearance` 文本 → 外观文字描述字段；`description` → 服装约束字段；`voiceId`（若已绑定）→ 指向 VoiceAsset 的引用保持不变
3. 其余角度和字段留空，待用户补全

**手动创建**：
1. 资产库 →「角色书」子 Tab → 点击「创建角色书」
2. 填写结构化外观字段

**外观字段结构**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `hairStyle` | string | 如 "黑色短发，偏分" |
| `faceShape` | string | 如 "清瘦，颧骨略高" |
| `skinTone` | string | 如 "偏白" |
| `build` | string | 如 "身高180cm，体型偏瘦" |
| `distinguishingFeatures` | string | 如 "左眉骨有一道浅疤" |
| `defaultClothing` | string | 如 "深蓝色长款风衣，内搭灰色高领毛衣，黑色长裤，皮质短靴" |
| `clothingColorScheme` | string | 如 "深蓝 + 灰 + 黑" |

#### 3.4.3 多角度图管理

**上传方式**：
- 拖拽图片到对应角度槽位
- 或点击槽位从本地选择图片
- 也可点击「生成」调用生图模型，使用角色外观描述 + 指定角度生成

**角度槽位**：

| 槽位 | key | 是否必须 | 自动选择条件 |
|------|-----|---------|-------------|
| 正面全身 | `front_full` | ✓ | 默认引用，所有镜头都可以用 |
| 侧面全身 | `side_full` | 推荐 | 镜头角度为侧身时自动选用 |
| 半身近景 | `bust_close` | 推荐 | 中景/近景镜头自动选用 |
| 表情特写 | `expression_close` | 可选 | 特写镜头自动选用 |
| 背面全身 | `back_full` | 按需 | 背面镜头自动选用 |

**自动选择逻辑**：分镜引用角色书时，系统根据分镜的 `camera.shotSize` 和 `camera.angle` 选择最匹配的参考图：
- `shotSize: full / wide` → 正面或侧面全身照
- `shotSize: medium` → 半身近景
- `shotSize: close_up / extreme_close_up` → 表情特写
- `angle: 侧向` → 侧面照
- 优先选择已生成的角度图，其次降级为文字描述

#### 3.4.4 跨分镜引用机制

**引用流程**：

1. 分镜生图时，系统读取分镜的 `characterRefs` 列表（角色书 ID 数组）。
2. 对每个 `ref_id`，查找对应 CharacterBook，提取：
   - 最匹配角度的参考图（Base64 或本地路径）
   - 外观文字描述（所有结构化字段拼接为一段角色提示词）
3. 组装到分镜的三类提示词中的「角色提示词」，设置 `region: "person_bbox"`。
4. 用户无需在每个分镜中重写角色描述。

**降级策略**：
- 角色书尚未创建 → 降级使用 V1.0 的 CharacterAsset 设定图 + 文字描述
- 角色书的某个角度图缺失 → 降级使用最接近角度（如无侧面照则用正面照代替，并在提示词中注明"侧身"）
- 角色书完全不存在 → 纯文本提示词，toast 提示「建议创建角色书以保证形象一致性」

#### 3.4.5 声音维度与角色卡导出（V1.2+）

在角色书界面中，声音作为与外观、服装并列的维度展示：

1. 系统通过 `voiceId` 自动从 VoiceAsset 表拉取关联的音色数据。
2. 角色书的声音区域展示：
   - **音色名称**（如"深沉男声-林墨"）
   - **试听按钮 ▶**：播放缓存音频文件
   - **TTS 参数**：语速、音调
   - **所属引擎**：标识音色来源（OpenAI TTS / Edge TTS 等）
3. 用户可在此处点击「更换音色」，从音色资产列表中重新选择。

**角色卡导出**：

当用户导出角色书时，系统将以下内容打包为一个完整的独立实体：

| 打包内容 | 说明 |
|---------|------|
| 角色基础信息 | 姓名、性别、年龄等 |
| 结构化外观 JSON | 发型、脸型、肤色、体型、标志性特征 |
| 服装约束 JSON | 常驻服装 + 颜色方案 |
| 多角度参考图 | 正面/侧面/半身/表情/背面图片文件 |
| **音色配置 JSON** | VoiceAsset 的完整字段（voiceId, provider, parameters） |
| **试听音频文件** | 缓存的试听 MP3 文件 |

导出格式为 ZIP 包，内含 `character.json` + `images/` + `voice/` 目录。其他用户导入此 ZIP 后，角色书即包含完整的外观和声音定义，无需重新配置即可在分镜中引用。

---

### 3.5 场景标注（V1.2+）

> **[V1.2+]** 本章描述的功能在 V1.0 中不可用。V1.0 用户通过资产库的「场景概念图」（§3.3.4）以简化方式实现环境参考。

#### 3.5.1 标注界面设计

**入口**：资产库 → 右键场景概念图条目 →「场景标注」。进入标注模式后，界面切换为三栏布局：

```
┌─工具栏───────────────────────────────────────────────────────┐
│ [BBox] [深度] [光源] [标签] [撤销] [重做] [保存标注] [退出]   │
└──────────────────────────────────────────────────────────────┘
┌────────────┬──────────────────────────┬─────────────────────┐
│  工具属性   │                          │  对象属性面板        │
│            │      画布（场景图）        │                     │
│  BBox 模式 │                          │  选中对象: door_01  │
│  · 矩形    │    [场景概念图显示]        │  类型: door        │
│  · 多边形  │    [标注叠加层显示]        │  bbox: x0.12 y0.08  │
│            │                          │        w0.15 h0.35  │
│  深度赋值  │                          │  depth: 0.3         │
│  · 笔刷    │                          │  layer: 1           │
│  · 填充    │                          │  occlusion: none    │
│            │                          │                     │
│  光源标记  │                          │  标签:               │
│  · 点光源  │                          │  medieval, wood,     │
│  · 方向光  │                          │  arched              │
│            │                          │                     │
│  标签工具  │                          │  [删除对象]          │
│  · 添加    │                          │                     │
└────────────┴──────────────────────────┴─────────────────────┘
```

#### 3.5.2 硬约束标记

**标记对象类型**：

| 类型 | 示例 | 约束强度说明 |
|------|------|------------|
| `architecture` | 墙壁、天花板、地板 | 不可移动，不可变形 |
| `door` | 入口木门 | 位置固定，开合状态可变化 |
| `window` | 彩色玻璃窗 | 位置固定 |
| `furniture` | 长桌、椅子 | 位置固定，细微纹理可浮动 |
| `light_source` | 吊灯、壁灯 | 位置固定，光强可变化 |
| `prop` | 烛台、书本 | 位置可小幅移动 |

**标记操作流程**：
1. 选择「BBox」工具，在画布上拖拽绘制矩形（屏幕像素坐标自动归一化为 0-1）。
2. 在属性面板中选择对象类型，填写标签。
3. 为对象分配 `depth` 和 `layer` 值（见 §3.5.4）。

#### 3.5.3 软约束标签

在对象属性面板或全局标签面板中添加关键词标签：

- **全局标签**：描述场景整体风格、氛围
- **对象标签**：描述单个物体的材质、风格

标签以字符串数组存储，用于组装环境提示词中的修饰语部分。

#### 3.5.4 深度/层级标注

**深度值（depth）**：
- 范围 0–1，0 = 画面最远处（远景），1 = 画面最近处（前景）
- 标注方式：选中对象后在属性面板调整滑块；或使用「深度」笔刷在画布上涂抹赋值
- 对接 MiDaS / Depth Anything 等深度估计模型，可作为 ControlNet Depth 的输入条件

**层序值（layer）**：
- 整数值，数值越大越靠前（Z 排序）
- 同一 depth 范围的物体通过 layer 区分前后景
- 示例：桌子 depth=0.4 layer=2，人物 depth=0.6 layer=3 → 人物在桌子前面
- 直接映射为 ControlNet 或渲染引擎的深度层序

**交互关系（interaction）**：
- 当不同 layer 的物体发生物理接触时，在 JSON 中显式标记
- 格式：`{ with: "table_01", type: "hand_on_surface", contact_point: {x, y} }`
- 作用：告知生成模型这两个物体在该坐标点有前后景交叉，避免渲染错误

#### 3.5.5 按需补标流程

**触发条件**：
- 分镜的镜头角度与场景基准视角的偏移量 > 90°
- 分镜引用的场景区域在已有标注中覆盖率 < 60%

**补标流程**：
1. 分镜编辑界面中，标注未覆盖的分镜显示提示「⚠ 当前镜头角度超出场景标注覆盖范围」
2. 用户点击「补充标注」，打开标注界面，画布中央显示当前分镜预估的视角画面
3. 用户为盲区区域添加硬约束对象和软约束标签
4. 补充标注存储为 `SupplementaryBlock`，与原始标注合并
5. 后续该角度范围内的分镜自动继承补充标注

---

### 3.6 分镜管理

#### 3.6.1 剧本自动拆分分镜

**用户操作**：剧本生成后切换到「分镜」Tab，系统已自动拆分。

**实现逻辑（V1.0 简化拆分）**：
1. 遍历剧本的 `scenes` 数组。
2. 每场戏至少生成 1 个分镜。
3. 检测场次内的 `actionDescription` 中包含的动作转换关键词（"突然""随后""同时""另一边"），触发追加分镜。
4. 每个分镜自动填充：
   - `description`：来自场次的 `actionDescription` + 场景信息
   - `imagePrompt`：`{location}，{timeOfDay}，{description}，{characters}`
   - `sceneId`：关联到源场次
   - `status`：全部初始化为 `pending`

**实现逻辑（V1.2+ 智能拆分，通过 LLM）**：

1. 将全场次 JSON 发送给 LLM，由 LLM 输出分镜列表：

```
System: 你是一位专业分镜师。将剧本拆分为分镜列表。每个分镜需包含镜头参数。

User: 
## 剧本
{script JSON}

## 输出格式
[{ "sceneId": "...", "order": 1, "description": "...", "camera": { "shotSize": "medium", "angle": "eye_level", "movement": "dolly_in_slow" }, "characters": ["角色名"] }]
```

2. 解析 LLM 返回的分镜列表，写入项目的 `storyboards` 数组。

#### 3.6.2 手动编辑分镜

**操作**：
- 点击分镜 → 弹窗编辑（描述、镜头参数、时间轴）
- 拖拽调整顺序 → 更新 `order` 字段，重新编号
- 增删分镜 → 更新数组，重排 `order`
- 支持复制分镜（深拷贝，新 UUID）

#### 3.6.3 分镜提示词引擎

**V1.0 模式**：分镜携带单一 `imagePrompt` 和 `videoPrompt`。初始值由系统自动填充，用户手动修改。

自动填充逻辑：
```
imagePrompt = "{分镜描述}。{出场人物外观特征列表}。{场景描述}。{场景风格}"
```
其中人物外观和场景描述从匹配的资产库条目提取。

**V1.2+ 模式**：分镜携带三类独立提示词（`prompts` 对象）。自动组装逻辑：

| 提示词类型 | 组装逻辑 |
|-----------|---------|
| `environment` | 场景标注 JSON → 硬约束坐标格式化为"画面中门位于左侧三分之一处……" + 软约束标签拼接"中世纪哥特风格，石质墙面……" + 分镜的镜头参数"中景，眼平角度，缓慢推进" |
| `character` | `ref_id` → 角色书 → 最匹配角度参考图 + 结构化外观字段拼接。设置 `region: "person_bbox"` |
| `action_fx` | 从分镜描述中提取动态关键词（"渐亮""蔓延""飘落""闪烁"）→ 组装为动作/特效提示词。设置 `region: "fx_mask"` |

**两套模式切换**：
- 全局设置 > 勾选「启用锚定式分镜引擎（V1.2）」
- 关闭时使用 V1.0 模式，分镜仅展示单一提示词输入框
- 开启时使用 V1.2 模式，展示三类提示词编辑区
- 切换模式时不丢失数据：V1.0 的 `imagePrompt` 保留在原字段；V1.2 的 `prompts` 独立存储

#### 3.6.4 时间轴编辑

**编辑界面**：分镜编辑弹窗底部展示时间轴轨道。

```
┌─ 时间轴 ──────────────────────────────────────────────────────┐
│ 0s        1s        2s        3s        4s       4.5s        │
│ ├─────────┼─────────┼─────────┼─────────┼─────────┤          │
│ [█████████████ 动作: 主角推开门  ]  sync: door_creak          │
│               [█████████████████  fx: 符文渐亮  ] ease_in     │
│                         [█████████████████ 对白: 林墨台词  ]   │
│ [+ 添加事件]                                                  │
└──────────────────────────────────────────────────────────────┘
```

**操作**：
- 点击「+ 添加事件」在播放头位置插入事件节点
- 拖拽事件条调整 t_ms
- 拖拽事件条两端调整 duration_ms
- 双击事件条编辑属性（label, sync_hint, tts_anchor 等）

**实现逻辑**：
- Timeline 数据以 `TimelineEvent[]` 数组存储在 Storyboard 中
- 前端使用自定义时间轴组件渲染，换算 `durationMs / 像素` 比例
- 所有时间值单位为毫秒，精度到 100ms

#### 3.6.5 镜头参数配置

**V1.2+** 的分镜携带标准化镜头参数：

| 参数 | 可选值 | 说明 |
|------|--------|------|
| `shotSize` | `extreme_wide` / `wide` / `medium` / `close_up` / `extreme_close_up` | 景别 |
| `angle` | `low` / `eye_level` / `high` / `dutch` | 角度 |
| `movement` | `static` / `dolly_in` / `dolly_out` / `pan_left` / `pan_right` / `tilt_up` / `tilt_down` / `handheld` | 镜头运动 |

这些参数在组装环境提示词时转换为自然语言描述注入到环境中（如 `shotSize: "medium"` → "中景"），同时也用于角色书的参考图角度自动匹配。

---

### 3.7 生图模型接入

#### 3.7.1 功能说明

调用外部图像生成模型，生成人物设定图、场景概念图、物品设计图和分镜画面图。风格参考图为用户本地导入，不经过生图模型生成。

#### 3.7.2 接口设计

**内置模板**：

| 模板名 | 请求格式 |
|--------|---------|
| Stable Diffusion WebUI | `POST {baseUrl}/sdapi/v1/txt2img` → `{ prompt, negative_prompt, width, height, steps, ... }` |
| OpenAI DALL·E | `POST {baseUrl}/images/generations` → `{ model, prompt, n, size }` |
| 自定义 | 用户编写请求 body 模板 + 响应 JSONPath |

**参考图传入方式**（V1.0）：
- 将资产库中的设定图/概念图转为 Base64，嵌入请求 body 的 `reference_images` 字段
- 如模型不支持参考图，则在提示词中追加角色外观描述文本

**三类提示词传入方式（V1.2+）**：
- 环境提示词 → 模型的 `prompt` 主字段
- 角色提示词 → 模型支持 Regional Prompting 时传入 `regional_prompts[person_bbox]`
- 动作/特效提示词 → 模型支持 Regional Prompting 时传入 `regional_prompts[fx_mask]`
- 模型不支持时，三类提示词按权重拼接为单一 `prompt`，权重：环境 0.5 + 角色 0.3 + 动作 0.2

#### 3.7.3 边界情况

- API 返回错误：展示错误信息，批量任务跳过当前继续
- 图片保存失败：提示检查本地存储空间
- 参考图文件不存在：降级为纯文本模式，toast 提示
- 提示词安全性：不做内容审核，用户自行负责

---

### 3.8 视频生成模型接入

#### 3.8.1 功能说明

调用外部视频生成模型，以分镜图片为参考图，结合运动提示词，生成动态视频片段。

#### 3.8.2 接口设计

| 模板名 | 请求格式 |
|--------|---------|
| Runway Gen-3 | `POST {baseUrl}/v1/tasks` → 上传图 + prompt，轮询 `GET /v1/tasks/{id}` |
| Pika | `POST {baseUrl}/v1/generate` → `{ prompt, image_url, duration }` |
| Kling | `POST {baseUrl}/v1/videos/image2video` → 上传图 + prompt + duration |
| 自定义 | 用户编写请求 body 模板 + 响应 JSONPath |

#### 3.8.3 异步任务处理

1. 提交任务 → 获取 `task_id`
2. 轮询任务状态（间隔 3s，最大等待 10 分钟）
3. 状态变为 `completed` → 下载视频文件 → 转为 MP4 存储到本地
4. 状态变为 `failed` → 展示错误信息
5. 超时 → 提示「任务超时，请稍后手动刷新」

#### 3.8.4 边界情况

- 参考图未生成：按钮置灰，提示「请先生成图片」
- 视频格式兼容：不同模型返回格式不同（MP4 / WebM / GIF），统一转为 H.264 MP4
- 异步任务超时：设置 10 分钟最大等待，超时后任务 ID 保留，用户可手动刷新

---

### 3.9 语音模型接入

#### 3.9.1 功能说明

调用外部 TTS 模型，根据角色对白生成配音音频。

#### 3.9.2 接口设计

| 模板名 | 请求格式 |
|--------|---------|
| OpenAI TTS | `POST {baseUrl}/audio/speech` → `{ model, input, voice }` |
| Edge TTS | 本地调用 edge-tts CLI |
| 自定义 | 用户编写请求 body 模板 + 响应 JSONPath |

#### 3.9.3 角色音色映射

- 每个 Character 可绑定 `voiceId`（音色 ID）
- 在全局设置中预设音色 ID 与音色名称的映射表（如 `{ "voice_01": "深沉男声", "voice_02": "清亮女声" }`）
- 生成配音时，系统根据对白所属角色查找 `voiceId`，传入 TTS 接口
- 未配置音色则使用默认音色

#### 3.9.4 边界情况

- 对白文本为空：跳过
- 音色未配置：使用默认音色 + toast 提示
- 长文本超限：自动分段生成 → 拼接音频

---

### 3.10 视频合成与导出

#### 3.10.1 功能说明

将所有分镜视频片段按顺序拼接，叠加配音音频和字幕，导出为完整 MP4 文件。

#### 3.10.2 合成管线

```
分镜视频列表（按 order 排序）
    │
    ▼
[1] concat demuxer 拼接所有视频片段
    │
    ▼
[2] 音频叠加：各段配音按时间轴偏移混入音轨
    │
    ▼
[3] 字幕生成：根据对白文本生成 ASS 字幕文件
    │
    ▼
[4] 编码输出：H.264 + AAC 的 MP4 文件
```

#### 3.10.3 实现方式

- 主进程调用本地 FFmpeg（内置或用户指定路径）
- 合成进度通过 IPC 实时回传前端
- 字幕使用 ASS 格式，支持嵌入（硬编码）或外挂（softsub）

#### 3.10.4 音画时间轴对齐（V1.2+）

V1.2 中，分镜携带 `timeline` 时间轴。合成时系统自动：

1. 读取每个分镜的 `timeline` 数组
2. 提取 `type: "dialogue"` 的事件，找到对应配音音频文件
3. 按 `t_ms` 将配音精确放置在合成时间轴的对应位置
4. 提取 `type: "fx"` 且带 `sync_hint` 的事件 → 输出音效对齐清单，供用户后期添加

**背景音乐**：当前视频生成模型无法直接生成背景音乐。系统在合成时不添加背景音轨——用户需在后期剪辑软件中手动添加。导出时可在时间轴中预留一个空白音轨标记（`reserved_bgm`），方便对位。

#### 3.10.5 边界情况

- 某分镜视频未生成：跳过该片段并提示
- 某分镜配音未生成：该片段静音
- FFmpeg 未找到：提示安装或指定路径
- 合成失败：保留日志供排查

---

### 3.11 全局设置

#### 3.11.1 设置项

| 类别 | 设置项 | 说明 |
|------|--------|------|
| LLM | API Base URL + Key + 模型名称 + 最大 Token | 剧本生成和审查 |
| 生图模型 | API Base URL + Key + 默认尺寸 + 接口模板 | 支持 SD WebUI / DALL·E / 自定义 |
| 视频模型 | API Base URL + Key + 默认时长 + 接口模板 | 支持 Runway / Pika / Kling / 自定义 |
| 语音模型 | API Base URL + Key + 默认音色 + 接口模板 | 支持 OpenAI TTS / Edge TTS / 自定义 |
| 分镜引擎 | V1.0 / V1.2+ 模式切换 | 控制使用简化模式还是锚定模式 |
| 通用 | FFmpeg 路径、导出默认目录 | |

#### 3.11.2 API 密钥安全

- API Key 使用操作系统安全存储（macOS Keychain / Windows Credential Vault），不以明文写入本地文件
- 设置文件 `settings.json` 中仅存储非敏感配置

---

### 3.12 数据备份与恢复

#### 3.12.1 导出

- 将所有项目的元数据 + 资产库元数据序列化为一个 JSON 文件
- 关联媒体文件以 Base64 嵌入或与 JSON 一同打包为 ZIP
- 用户选择存储位置

#### 3.12.2 导入

- 选择 JSON/ZIP 文件，解析项目数据
- 以项目 UUID 去重，已存在则提示覆盖或跳过
- Base64 媒体数据解码并写入本地文件系统

---

## 4. 页面与交互设计

### 4.1 整体布局

```
┌──────────┬──────────────────────────────┬──────────────┐
│  项目    │         主编辑区               │   预览区     │
│  列表    │                              │              │
│          │  [大纲] [剧本] [分镜] [资产库] │  图片/视频   │
│  ┌────┐  │  ───────────────────────     │  预览播放    │
│  │项目1│  │                              │              │
│  │项目2│  │                              │              │
│  └────┘  │                              │              │
│ [+新建]  │                              │              │
└──────────┴──────────────────────────────┴──────────────┘
```

- **左侧面板**（240px）：项目列表，底部「+ 新建项目」
- **中间面板**（弹性宽度）：主编辑区，顶部 4 个 Tab（大纲 / 剧本 / 分镜 / 资产库）
- **右侧面板**（400px）：预览区，选中图片/视频时展示预览，音频时展示波形 + 播放控件

### 4.2 各 Tab 页面设计

**大纲 Tab**：
- 上方：大纲文本编辑区（textarea，占页面 60%）
- 下方：人物小传卡片列表（横向排列，可滚动）
- 底部工具栏：「添加人物」「审查大纲」「生成剧本」

**剧本 Tab**：
- 以场次为单位纵向排列
- 每场显示：地点 + 时间标签、场景描述、动作描述、对白列表
- 每场右上角：编辑/删除按钮

**分镜 Tab**：
- 左侧：分镜列表（缩略图 + 序号 + 状态标签）
- 右侧：选中分镜的详情（描述、三类提示词/单一提示词、时间轴、镜头参数）

**资产库 Tab**：

```
┌──────────────────────────────────────────────────────────────┐
│  🎨 资产库    [人物设定] [场景设定] [物品设计] [风格参考] [分镜生图] │
│                          [角色书(V1.2)] [场景标注(V1.2)]      │
│                                                              │
│  ┌─ 筛选 ──────────────────────────────────────────────────┐ │
│  │ 搜索...  [项目: 全部 ▾]  [状态: 全部 ▾]                   │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌──────────────────────┬──────────────────────────────────┐ │
│  │    资产列表（左侧）    │      预览 / 生成区（右侧）       │ │
│  │  👤 林墨  ✓ 已就绪   │  ┌────────────────────────┐     │ │
│  │  👤 配角A ○ 待生成   │  │    预览图 / 生成结果     │     │ │
│  │  🏙️ 古宅大厅 ✓      │  └────────────────────────┘     │ │
│  │  [+ 添加资产]        │  提示词: [___________________]   │ │
│  │                      │  [🔄 生成] [💾 保存]             │ │
│  └──────────────────────┴──────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

### 4.3 弹窗清单

| 弹窗 | 触发方式 | 内容 |
|------|---------|------|
| 新建/编辑项目 | 左侧新建按钮 / 右键菜单 | 项目名称输入 |
| 人物编辑 | 「添加人物」按钮 | 人物信息表单 |
| 分镜编辑 | 分镜列表 → 点击 | 描述、提示词、镜头参数、时间轴 |
| 视频生成设置 | 分镜 → 「生成视频」 | 运动提示词、时长 |
| 合成导出设置 | 工具栏「合成导出」 | 分辨率、帧率、字幕、输出路径 |
| 全局设置 | 工具栏齿轮图标 | 各模型 API 配置 |
| 删除确认 | 删除操作时 | 二次确认 |

### 4.4 状态反馈

| 场景 | 反馈方式 |
|------|---------|
| 生成中 | 按钮 loading + 进度文字 |
| 生成成功 | 目标区域刷新 + toast 「生成完成」 |
| 生成失败 | toast 错误原因 + 按钮恢复 |
| 批量任务 | 列表每项状态图标（⏳/✅/❌）+ 底部进度条 |
| 合成导出 | 进度条 + 预估剩余时间 |
| 保存/删除 | toast 操作结果 |

---

## 5. 数据模型

### 5.1 项目（Project）

```typescript
interface Project {
  id: string;                    // UUID
  name: string;                  // 项目名称
  outline: string;               // 剧情大纲（纯文本）
  characters: Character[];       // 人物小传列表
  script: Script | null;         // 生成的剧本
  storyboards: Storyboard[];     // 分镜列表
  createdAt: string;             // ISO 8601
  updatedAt: string;             // ISO 8601
}
```

### 5.2 人物（Character）

```typescript
interface Character {
  id: string;
  name: string;
  gender: 'male' | 'female' | 'other';
  age: number | null;
  personality: string;           // 性格描述
  background: string;            // 背景故事
  appearance: string;            // 外观特征（自由文本，V1.2 中可同步到角色书的结构化字段）
  voiceId: string | null;        // 绑定的音色 ID
}
```

### 5.3 剧本 / 场次 / 对白

```typescript
interface Script {
  title: string;
  scenes: Scene[];
}

interface Scene {
  id: string;
  sceneNumber: number;
  location: string;              // 地点
  timeOfDay: string;             // 时间（"日"/"夜"/"黄昏"）
  description: string;           // 场景描述
  characters: string[];          // 出场人物名称列表
  actionDescription: string;     // 动作描述
  dialogues: Dialogue[];
}

interface Dialogue {
  id: string;
  characterName: string;
  text: string;
  order: number;
}
```

### 5.4 分镜（Storyboard）

```typescript
interface Storyboard {
  id: string;
  sceneId: string;               // 关联场次 ID
  order: number;

  // === [V1.0] 简化提示词 ===
  description: string;           // 分镜描述
  imagePrompt: string;           // 生图提示词（V1.0）
  videoPrompt: string;           // 视频运动提示词

  // === [V1.2+] 锚定式提示词 ===
  prompts?: {
    environment: {
      text: string;
      region: 'full';
    };
    character: {
      ref_id: string;            // 指向 CharacterBook.id
      region: 'person_bbox';
    };
    action_fx: {
      text: string;
      region: 'fx_mask';
      attention_isolation: boolean;
    };
  };
  camera?: {                     // V1.2+ 镜头参数
    shotSize: 'extreme_wide' | 'wide' | 'medium' | 'close_up' | 'extreme_close_up';
    angle: 'low' | 'eye_level' | 'high' | 'dutch';
    movement: string | null;
  };
  timeline?: TimelineEvent[];    // V1.2+ 时间轴

  // === 锚定引用 ===
  sceneRef?: string;             // V1.2+ 场景标注 ID
  characterRefs?: string[];      // V1.2+ 角色书 ID 列表

  // === 生成结果 ===
  imagePath: string | null;
  videoPath: string | null;
  audioPath: string | null;
  durationMs: number;            // 毫秒，默认 5000

  referenceAssets: {
    characterImages: string[];
    sceneImages: string[];
    itemImages: string[];
  };

  status: {
    image: 'pending' | 'generating' | 'done' | 'failed';
    video: 'pending' | 'generating' | 'done' | 'failed';
    audio: 'pending' | 'generating' | 'done' | 'failed';
  };
}

interface TimelineEvent {
  t_ms: number;
  type: 'action' | 'fx' | 'dialogue';
  label: string;
  duration_ms: number;
  sync_hint?: string;
  tts_anchor?: boolean;
  intensity_curve?: 'ease_in' | 'ease_out' | 'linear' | 'bell';
}
```

### 5.5 全局资产库

```typescript
interface AssetStore {
  characters: CharacterAsset[];
  scenes: SceneAsset[];
  items: ItemAsset[];
  voices: VoiceAsset[];            // 音色资产
  styles: StyleReferenceAsset[];   // 外部导入的风格参考图
}

interface CharacterAsset {
  id: string;
  name: string;
  characterId: string | null;
  description: string;
  designImagePath: string | null;
  source: 'generated' | 'imported';  // 图片来源：AI 生成 或 本地导入
  isPrimary: boolean;
  createdAt: string;
  updatedAt: string;
}

interface SceneAsset {
  id: string;
  name: string;
  description: string;
  conceptImagePath: string | null;
  source: 'generated' | 'imported';
  createdAt: string;
  updatedAt: string;
}

interface ItemAsset {
  id: string;
  name: string;
  description: string;
  designImagePath: string | null;
  source: 'generated' | 'imported';
  relatedCharacterIds: string[];
  createdAt: string;
  updatedAt: string;
}

/** 音色资产 —— V1.0 建表，V1.2 角色书聚合展示 */
interface VoiceAsset {
  id: string;
  name: string;                  // 音色名称（如"深沉男声-林墨"）
  voiceId: string;               // TTS 引擎的音色标识符（如 openai-tts 的 "onyx"）
  provider: 'openai-tts' | 'edge-tts' | 'custom';  // 所属 TTS 引擎
  sampleText: string;            // 试听文本
  sampleAudioPath: string | null; // 试听音频缓存路径（调用 TTS 生成后缓存）
  parameters: {
    speed: number;               // 语速 0.5–2.0，默认 1.0
    pitch: number;               // 音调 -20–20，默认 0
  };
  createdAt: string;
  updatedAt: string;
}

/** [V1.0] 外部导入的风格参考图 */
interface StyleReferenceAsset {
  id: string;
  name: string;                  // 风格名称（如"赛博朋克霓虹风"）
  description: string;           // 风格描述
  imagePath: string;            // 导入的风格图片本地路径
  styleStrength: number;        // 风格强度 0–1，默认 0.7
  createdAt: string;
  updatedAt: string;
}
```

### 5.6 角色书（CharacterBook）[V1.2+]

```typescript
interface CharacterBook {
  id: string;
  name: string;

  /** 结构化外观字段 */
  appearance: {
    hairStyle: string;
    faceShape: string;
    skinTone: string;
    build: string;
    distinguishingFeatures: string;
  };

  /** 服装约束 */
  clothing: {
    defaultClothing: string;
    colorScheme: string;
  };

  /** 多角度参考图 */
  referenceImages: {
    front_full: string | null;     // 正面全身 —— 必须
    side_full: string | null;      // 侧面全身
    bust_close: string | null;     // 半身近景
    expression_close: string | null; // 表情特写
    back_full: string | null;      // 背面全身
  };

  /** 姿态参考 */
  poseReferences: {
    description: string;           // 站姿/行走姿态等文字描述
    images: string[];              // 可选的姿态参考图
  };

  /** 声音锚定（通过 voiceId 引用 VoiceAsset） */
  voiceId: string | null;          // V1.0 已建，V1.2 角色书界面聚合展示

  createdAt: string;
  updatedAt: string;
}
```

### 5.7 场景标注（SceneAnnotation）[V1.2+]

```typescript
interface SceneAnnotation {
  id: string;
  name: string;
  description: string;

  lighting: {
    primary: {
      direction: string;          // 如 "top_right_45deg"
      type: 'sunlight' | 'ambient' | 'point' | 'spot';
      colorTemp: number;          // 色温（开尔文）
      intensity: number;          // 0–1
    };
    secondary: LightSource[];
  };

  hardConstraints: HardConstraint[];
  softConstraints: {
    style: string[];              // ["medieval", "gothic", "abandoned"]
    materials: string[];          // ["stone_wall", "wooden_floor"]
    atmosphere: string[];         // ["dusty", "dim", "solemn"]
    details: string[];            // ["cobweb", "cracked_tile"]
  };

  referenceView: {
    cameraPosition: { x: number; y: number; z: number };
    lookAt: { x: number; y: number; z: number };
    fov: number;
  };

  supplementaryBlocks: SupplementaryBlock[];
  createdAt: string;
  updatedAt: string;
}

interface HardConstraint {
  id: string;
  type: 'architecture' | 'furniture' | 'prop' | 'light_source' | 'door' | 'window';
  label: string;                  // 人类可读标签
  bbox: { x: number; y: number; w: number; h: number }; // 归一化 0–1
  depth: number;                  // 0–1, 0=最远
  layer: number;                  // Z 层级，越大越靠前
  occlusion: 'none' | 'partial' | 'full';
  interaction?: {
    with: string;                 // 交互目标对象 ID
    type: string;                 // 如 "hand_on_surface"
    contact_point: { x: number; y: number };
  };
}

interface LightSource {
  direction: string;
  type: 'ambient' | 'point' | 'spot';
  colorTemp: number;
  intensity: number;
}

interface SupplementaryBlock {
  id: string;
  triggerAngle: number;           // 触发补标的视角偏移量
  viewDescription: string;
  hardConstraints: HardConstraint[];
  softConstraints: {
    style: string[];
    materials: string[];
    details: string[];
  };
}
```

### 5.8 全局设置（Settings）

```typescript
interface Settings {
  llm: { baseUrl: string; apiKey: string; model: string; maxTokens: number; };
  imageModel: { baseUrl: string; apiKey: string; defaultSize: string; template: string; customHeaders: Record<string, string>; };
  videoModel: { baseUrl: string; apiKey: string; defaultDuration: number; template: string; customHeaders: Record<string, string>; };
  tts: { baseUrl: string; apiKey: string; defaultVoice: string; template: string; customHeaders: Record<string, string>; };
  storyboardEngine: 'v1.0' | 'v1.2';  // 分镜引擎版本
  ffmpegPath: string | null;
  exportPath: string;
}
```

### 5.9 存储方案

| 数据类型 | 存储位置 | 说明 |
|---------|---------|------|
| 项目元数据 | `userData/projects.json` | 所有项目合并存储 |
| 项目媒体文件 | `userData/projects/{projectId}/media/` | 分镜图、视频、音频 |
| 全局资产库 | `userData/assets/asset-store.json` | 独立于项目 |
| 资产设定图 | `userData/assets/{分类}/{名称}/design.png` | |
| 风格参考图 | `userData/assets/风格参考/{名称}/style.png` | 用户从本地导入，非生成 |
| 音色资产 JSON | `userData/assets/voices.json` | VoiceAsset 元数据 |
| 音色试听音频 | `userData/assets/音色/{名称}/sample.mp3` | TTS 生成后缓存 |
| 角色书 JSON | `userData/character-books/{id}.json` | V1.2+ |
| 角色书图片 | `userData/character-books/{id}/images/` | V1.2+ |
| 场景标注 JSON | `userData/scene-annotations/{id}.json` | V1.2+ |
| 全局设置 | `userData/settings.json` | API Key 存系统密钥链 |
| 导出备份 | 用户指定目录 | JSON + 可选媒体文件打包 ZIP |

---

## 6. 技术方案

### 6.1 技术栈

| 层级 | 技术选型 |
|------|---------|
| 桌面框架 | Electron |
| 前端框架 | React + TypeScript |
| 状态管理 | Zustand |
| UI 组件库 | Ant Design / shadcn/ui |
| 本地存储 | electron-store + 直接 JSON 读写 |
| 文件系统 | Node.js fs API（主进程） |
| FFmpeg | fluent-ffmpeg + 内置 FFmpeg 二进制 |
| 打包 | electron-builder |
| API 通信 | fetch / axios（渲染进程直接调用） |

### 6.2 进程架构

```
┌─────────────────────────────────────────────────┐
│                   Electron                       │
│                                                  │
│  ┌──────────────────┐   ┌────────────────────┐  │
│  │   渲染进程 (React)│   │   主进程 (Node.js)  │  │
│  │                  │   │                    │  │
│  │  - UI 渲染       │◄─►│  - 文件读写         │  │
│  │  - 用户交互      │IPC│  - FFmpeg 调用      │  │
│  │  - 调用外部 API  │   │  - 系统密钥链       │  │
│  │                  │   │  - 窗口管理         │  │
│  └──────────────────┘   └────────────────────┘  │
│                                                  │
│  外部通信：渲染进程直接调 AI 模型 API（HTTP）      │
│  本地操作：渲染进程通过 IPC 委托主进程执行          │
└─────────────────────────────────────────────────┘
```

### 6.3 IPC 通信接口

| 频道 | 方向 | 用途 |
|------|------|------|
| `project:save` | 渲染 → 主 | 保存项目 JSON |
| `project:load-all` | 渲染 → 主 | 加载所有项目 |
| `project:delete` | 渲染 → 主 | 删除项目及媒体文件 |
| `file:save-media` | 渲染 → 主 | 保存图片/视频/音频 |
| `file:read-media` | 渲染 → 主 | 读取媒体文件 |
| `file:export-zip` | 渲染 → 主 | 导出 ZIP |
| `file:import-zip` | 渲染 → 主 | 从 ZIP 导入 |
| `ffmpeg:compose` | 渲染 → 主 | 调用 FFmpeg 合成 |
| `ffmpeg:progress` | 主 → 渲染 | 合成进度回传 |
| `settings:save` | 渲染 → 主 | 保存设置 |
| `settings:load` | 渲染 → 主 | 加载设置 |
| `keychain:store` | 渲染 → 主 | 安全存储密钥 |
| `keychain:get` | 渲染 → 主 | 读取密钥 |

### 6.4 模型接入接口规范

所有外部模型通过「通用配置 + 模板适配」方式接入。模板定义请求 body 构造规则和响应解析路径。

**LLM**：`POST {baseUrl}/chat/completions`，兼容 OpenAI Chat Completions 格式。

**生图**：支持 Stable Diffusion WebUI、DALL·E、自定义。V1.2+ 的 Regional Prompting 参数映射到各模型的对应字段（如 SD WebUI 的 `regional_prompts` 扩展）。

**视频**：支持 Runway、Pika、Kling、自定义。异步任务统一轮询逻辑。

**语音**：支持 OpenAI TTS、Edge TTS、自定义。

### 6.5 FFmpeg 合成管线

1. **concat demuxer**：生成 `filelist.txt`，使用 `-f concat -safe 0` 拼接视频
2. **音频叠加**：各配音文件按时间偏移使用 `adelay` 滤镜混入
3. **字幕**：动态生成 ASS 字幕文件，使用 `subtitles` 滤镜嵌入
4. **编码**：`-c:v libx264 -c:a aac -preset medium` 输出 MP4

---

## 7. 开发计划

### 7.1 V1.0 —— 核心创作闭环（MVP）

**目标**：走通「大纲 → 剧本 → 分镜 → 生图 → 视频 → 配音 → MP4」完整闭环。

| 阶段 | 内容 | 预估 |
|------|------|------|
| 项目初始化 | Electron + React 脚手架，IPC 通信打通 | 2 天 |
| 项目管理 | 新建/删除/切换项目，本地 JSON 读写 | 2 天 |
| 剧本生成 | 大纲编辑、人物管理、LLM 接入、剧本编辑、大纲审查 | 4 天 |
| 资产库 | 人物设定图、场景概念图、物品设计图、风格参考导入与生图 | 3 天 |
| 分镜管理 | 剧本自动拆分、手动编辑、分镜生图（简化模式） | 3 天 |
| 视频生成 | 图转视频接口 + 异步轮询 | 3 天 |
| 语音合成 | TTS 接口接入 + 角色音色映射 | 2 天 |
| 预览区 | 图片/视频/音频预览播放 | 2 天 |
| 合成导出 | FFmpeg 拼接 + 配音 + 字幕 + 导出 MP4 | 3 天 |
| 全局设置 | API 配置、密钥安全存储 | 2 天 |
| 导入导出 | JSON/ZIP 备份恢复 | 2 天 |
| 联调测试 | 全流程走通，边界情况处理 | 3 天 |

**V1.0 总计：约 31 人天**

### 7.2 V1.1 —— 体验增强

| 功能 | 说明 |
|------|------|
| 自动备份 | 定时备份到指定目录 |
| 快捷键 | Ctrl+N/S/E/Z 等 |
| 提示词自动优化 | 调用 LLM 润色生图/视频提示词 |
| 音色试听 | 预设音色的试听播放 |
| 操作历史 / 撤销 | 文本编辑的 undo/redo |
| 资产库批量操作 | 批量删除、批量迁移资产 |

### 7.3 V1.2 —— 锚定机制升级

**目标**：引入角色书 + 场景标注 + 三类提示词引擎，实现跨镜头角色/场景一致性。

| 阶段 | 内容 | 预估 |
|------|------|------|
| 角色书模块 | CharacterBook 数据模型、创建/编辑 UI、多角度图管理、从 V1.0 资产升级迁移 | 5 天 |
| 场景标注模块 | 标注界面（BBox 绘制、深度赋值、标签管理）、SceneAnnotation 存储、按需补标 | 6 天 |
| 分镜引擎升级 | 三类提示词自动组装逻辑、V1.0/V1.2 模式切换、大纲→分镜 LLM 智能拆分 | 4 天 |
| 时间轴编辑 | 时间轴 UI 组件、TimelineEvent 管理、sync_hint 标签系统 | 3 天 |
| 音画对齐 | 时间轴驱动的配音对齐、音效标签输出清单 | 2 天 |
| 模型接口扩展 | Regional Prompting 参数映射、深度图输入对接 | 3 天 |
| 联调测试 | V1.0 数据迁移 + 锚定流程全链路测试 | 3 天 |

**V1.2 总计：约 26 人天**

**从 V1.0 升级到 V1.2 的兼容性**：
- V1.0 的 `CharacterAsset` 可一键升级为 `CharacterBook`
- V1.0 的 `SceneAsset` 可关联创建 `SceneAnnotation`
- V1.0 的 `Storyboard.imagePrompt` 保留，V1.2 的 `prompts` 为新增可选字段
- 用户在全局设置中切换 `storyboardEngine: 'v1.0' | 'v1.2'`，不影响已有数据

---

## 8. 附录

### 8.1 术语定义

| 术语 | 定义 |
|------|------|
| 剧情大纲 | 一段文字，描述故事的整体框架、情节走向和关键事件 |
| 人物小传 | 每个主要角色的详细设定，包括性格、背景、外貌 |
| 剧本 | 结构化影视文本，按场次组织，包含场景描述、动作指示和对白 |
| 分镜 | 剧本中一个具体画面的描述单元，对应一张生成图片和一个视频片段 |
| 提示词（Prompt） | 输入给 AI 模型的文字指令 |
| 资产库 | 全局共享的视觉与声音资产集合（人物设定图、场景概念图、物品设计图、音色、风格参考图） |
| 音色资产 | TTS 音色配置的独立实体，含音色 ID、试听音频、语速/音调参数。角色通过 VoiceID 引用 |
| 风格参考图 | 用户从本地导入的外部风格图片，用于锚定全部或部分分镜的视觉画风 |
| 角色卡 | 角色书的完整导出包，含外观 JSON + 参考图 + 音色配置 + 试听音频，导入即可用 |
| 角色书（V1.2+） | 多角度人物参考图 + 结构化外观字段的集合，用于跨分镜锚定角色形象 |
| 场景标注（V1.2+） | 场景的结构化 JSON 描述（硬约束坐标 + 软约束标签），一次标注所有分镜复用 |
| 硬约束 | 生成时必须精确匹配的场景元素（门窗位置、空间结构等） |
| 软约束 | 生成时允许小幅变化的场景元素（材质纹理、装饰细节等） |
| 三类提示词（V1.2+） | 环境提示词 + 角色提示词 + 动作/特效提示词，各自携带区域作用域 |
| 时间轴（Timeline） | 分镜内各事件的时间戳序列，用于音效/口型同步对齐 |
| 区域隔离 | 防止不同提示词之间的特征污染，通过 `region` 字段限定作用域 |

### 8.2 设计决策汇总

| # | 决策点 | 选择 | 理由 |
|---|--------|------|------|
| 1 | 场景标注格式 | 结构化 JSON | 机器可读，对接 ControlNet 等精确控制方案 |
| 2 | 约束粒度 | 分层标注：硬约束坐标 + 软约束标签 | 结构件不可变（防穿帮），装饰件允许浮动 |
| 3 | 动态要素归属 | 独立为动作/特效提示词，三类并列 | 职责清晰，便于区域隔离 |
| 4 | Prompt Bleeding 防护 | 预留 `region` + `attention_isolation` | 模型支持时直接启用 |
| 5 | 深度/遮挡表达 | 硬约束必带 `depth` + `layer` + `occlusion` | 2D BBox 无法表达前后景关系 |
| 6 | 标注盲区 | 按需补标 | 不预标 360°（成本高），不完全依赖模型推断（不稳定） |
| 7 | 角色书可移植性 | 只存素材 + 描述，不绑定编码 | 编码格式随模型而异，推迟到生成时按需提取 |
| 8 | 音画集成 | 架构预留，时间戳骨架就位 | 技术高速迭代，不急于绑定方案 |
| 9 | 时间轴锚点 | `timeline` + `sync_hint` 语义标签 | 与具体引擎解耦 |
| 10 | 风格图来源 | 外部导入（本地图片），不经 AI 生成 | 风格图需要精确还原目标画风，AI 生成不可控；用户自行收集 |

### 8.3 风险与约束

- **API 稳定性**：外部 AI 服务可能宕机或变更接口。系统需优雅处理错误，避免数据丢失。
- **费用**：所有模型调用由用户自行申请和付费，应用仅做请求转发。
- **版权**：生成内容的版权归属取决于各模型服务商条款，应用不参与版权判定。
- **离线使用**：项目管理、剧本编辑、分镜管理、视频合成等可离线；仅调用外部模型需联网。
- **资产体积**：图片自动压缩（≤5MB/张），总资产库超 500MB 时提示清理。
